type Query {
    # Account
    currentAccount: Account
    accounts: [Account!]

    # Note
    notes(
        offset: Int
        limit: Int
        keyword: String
    ): [Note!]

    # Tag
    tags(keyword: String): [Tag!]
}